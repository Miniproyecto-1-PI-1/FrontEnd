# URL del backend (Spring Boot). En local: http://localhost:8080
VITE_API_URL=http://localhost:8080

# false = usa el backend real en VITE_API_URL (valor por defecto).
# true  = datos simulados en memoria, solo para maquetar sin backend.
VITE_USE_MOCK=false
