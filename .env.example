# URL del backend (Spring Boot). En local: http://localhost:8080
VITE_API_URL=http://localhost:8080

# true  = datos simulados en memoria (sin backend). Es el valor por defecto.
# false = usa el backend real en VITE_API_URL
VITE_USE_MOCK=true
